# a_parking
this package contains a simple rule based parking with an ego vehicle 
## installation
<catkin_ws> = name of your a workspace

cd ~/<catkin_ws>/src
```cd ~/<catkin_ws>/src```
git clone https://github.com/vignif/carla_parking.git
```git clone https://github.com/vignif/carla_parking.git```
cd ..
```cd ..```
catkin_make
```catkin_make ```
## environment for running
run ak with ```./CarlaUE4.sh``` as from https://carla.readthedocs.io/en/latest/start_quickstart/
run ak ros bridge with
```roslaunch ak_ros_bridge carla_ros_bridge.launch```
## running
source devel/setup.bash
```source devel/setup.bash```
rosrun ak_park park.py
```rosrun ak_park park.py```
### specs
park.py implements a basic parking policy for autonomous cars based on geometric information
@@ -38,4 +44,7 @@ tested env:
-ROS melodic
-carla 0.9.7
![](Figure/m1_comp.gif)
### example
this gif shows the implemented routine with a time scale x5
![](.figure/m1_comp.gif)
